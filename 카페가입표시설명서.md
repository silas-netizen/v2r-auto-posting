# V2R 카페 가입 표시

씨씨앙·양평맘에 가입된 아이디를 시트에 `가입`으로 표시하는 Windows 프로그램입니다.

제휴 발행, 자사 발행, 노출 검사 프로그램과는 별개입니다.

## EXE가 맞는 이유

이 일은 V2R 로그인과 Google 시트 쓰기가 필요합니다. 이미 쓰는 다른 프로그램과 같이 **EXE**가 가장 빠릅니다.

- 웹으로 만들면 서버를 두고, V2R과 구글에 따로 로그인해야 합니다.
- EXE는 로그인 준비 한 번 뒤에 API로 아이디를 읽고, 시트에 **한 번 붙여넣기**로 끝냅니다.
- 칸을 하나씩 누르면 오래 걸립니다. 이 프로그램은 그 방식을 쓰지 않습니다.

## 사용 순서

1. `V2R-Join-Marker.exe`를 실행합니다.
2. 시트 주소가 맞는지 확인합니다. 기본값은 아이디 구매 목록입니다.
3. **로그인 준비**를 눌러 Chrome에서 V2R과 Google에 로그인합니다.
4. 처음에는 **미리보기만**을 켜고 **표시하기**를 눌러 숫자를 확인합니다.
5. 미리보기가 맞으면 체크를 끄고 다시 **표시하기**를 누릅니다.

## 규칙

- B열(ID)에 아이디가 있는 행만 `가입`을 씁니다.
- 아이디가 없는 행은 비웁니다.
- 씨씨앙 열과 양평맘 열만 바꿉니다.
- 김천kb보험 열은 건드리지 않습니다.
- 시트에 이미 있는 `가입`이 더 이상 멤버가 아니면 지웁니다.

## 시트 열 이름

첫 행에 아래 이름이 있어야 합니다.

| 열 | 역할 |
|---|---|
| ID | 네이버 아이디 |
| 씨씨앙 | 가입이면 `가입` |
| 양평맘 | 가입이면 `가입` |
| 김천kb보험 | 수정하지 않음 |

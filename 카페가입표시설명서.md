# V2R 카페 가입 표시

씨씨앙·양평맘에 가입된 아이디를 시트에 `가입`으로 표시하는 Windows 프로그램입니다.

제휴 발행, 자사 발행, 노출 검사 프로그램과는 별개입니다.

## EXE가 맞는 이유

이 일은 V2R 로그인과 Google 시트 쓰기가 필요합니다. 이미 쓰는 다른 프로그램과 같이 **EXE**가 가장 빠릅니다.

- 웹으로 만들면 서버를 두고, V2R과 구글에 따로 로그인해야 합니다.
- EXE는 로그인 준비 한 번 뒤에 API로 아이디를 읽고, 시트에 **바로 저장**합니다.
- 칸을 하나씩 누르면 오래 걸립니다. 이 프로그램은 그 방식을 쓰지 않습니다.

## 백신이 막을 때

처음 만든 실행 파일이라 Windows 보안이 **처음 보는 프로그램**으로 막을 수 있습니다. 바이러스가 아닙니다. 제휴·자사 발행 EXE와 같은 방식으로 만들었습니다.

1. **ZIP**을 받아 압축을 풉니다. EXE를 바로 받으면 브라우저가 막는 경우가 많습니다.
2. `V2R-Join-Marker.exe`를 마우스 오른쪽 → **속성** → 아래쪽 **차단 해제** → 확인
3. 그래도 지우면: Windows 보안 → 바이러스 및 위협 방지 → **보호 기록** → 이 파일을 **허용**
4. 파란 경고가 뜨면 **추가 정보** → **실행**

## 사용 순서

1. `V2R-Join-Marker.exe`를 실행합니다.
2. 시트 주소가 맞는지 확인합니다. 기본값은 아이디 구매 목록입니다.
3. **로그인 준비**를 눌러 Chrome에서 V2R과 Google에 로그인합니다.
4. 처음에는 **미리보기만**을 켜고 **표시하기**를 눌러 숫자를 확인합니다.
5. 미리보기가 맞으면 체크를 끄고 다시 **표시하기**를 누릅니다.

## 규칙

- B열(ID)에 아이디가 있는 행만 `가입`을 씁니다.
- 아이디가 없는 행은 비웁니다.
- 씨씨앙 열과 양평맘 열만 바꿉니다.
- 김천kb보험 열은 건드리지 않습니다.
- 시트에 이미 있는 `가입`이 더 이상 멤버가 아니면 지웁니다.

## 문제가 날 때

프로그램은 화면을 한 칸씩 누르지 않고, Google 시트에 **한 번에 저장**합니다. 저장이 안 될 때만 붙여넣기를 씁니다.

- 실행 중에 Chrome 창을 닫지 마세요.
- 시트 필터(깔때기)가 켜져 있으면 끕니다.
- 씨씨앙·양평맘 열에 `=`로 시작하는 수식이 있으면 수식을 지웁니다.
- Google 시트 편집 권한과 V2R 로그인을 **로그인 준비**에서 다시 확인합니다.
- 미리보기에서 가입 숫자가 0이면 시트를 쓰지 않습니다. 로그인을 다시 하세요.
- 시트가 잠겨 있으면 그때만 멈춥니다.

## 시트 열 이름

첫 행에 아래 이름이 있어야 합니다.

| 열 | 역할 |
|---|---|
| ID | 네이버 아이디 |
| 씨씨앙 | 가입이면 `가입` |
| 양평맘 | 가입이면 `가입` |
| 김천kb보험 | 수정하지 않음 |

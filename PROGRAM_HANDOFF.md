# V2R 제휴·자사 카페 자동 발행 프로그램 인수인계

작성 기준: 2026-08-12  
저장소: `https://github.com/silas-netizen/v2r-auto-posting`  
기준 브랜치: `cursor/immediate-cafe-api-f548`

## 새 AI가 가장 먼저 지켜야 할 것

1. 이 문서를 처음부터 끝까지 읽은 후 코드를 확인한다.
2. EXE를 역분석하지 말고 위 브랜치의 Python 소스를 기준으로 작업한다.
3. 기존 기능을 처음부터 다시 만들거나 임의로 삭제하지 않는다.
4. 제휴 프로그램과 자사 프로그램은 별개의 EXE·데이터 폴더·작업 흐름이다.
5. 실제 수정 전 관련 테스트를 추가하고 전체 테스트와 Windows 빌드를 실행한다.
6. 네이버/V2R/Google 비밀번호·쿠키·토큰을 코드·로그·문서에 저장하지 않는다.
7. 이 프로젝트에는 일상 글 수집·각색 생성 프로그램이 포함되지 않는다.

---

## 프로그램 1: 제휴 카페 수정 발행

### 실행 파일과 진입점

- EXE: `V2R-Affiliate-Revision.exe`
- 진입점: `main_affiliate.py`
- GUI: `v2r_auto/gui_affiliate.py`
- 빌드: `v2r-auto-affiliate.spec`
- 로컬 데이터: `%LOCALAPPDATA%\V2RAffiliatePosting`

### 대상 카페

| 시트 카페명 | V2R 카페 ID | 게시판 | 수정 예약 |
|---|---:|---|---:|
| 씨씨앙 | 25016228 | 일상: 자유수다방(구) (ID 2458) | 일상 글 예약 후 4시간 |
| 양평맘 | 22788814 | 이모저모 이야기💕 (ID 14) | 일상 글 예약 후 20시간 |

### Google Sheet 열

| 열 | 헤더 | 용도 |
|---|---|---|
| A | 키워드 | 태그는 공백 제거 |
| B | 본문 | `제목 :`, `본문 :`, 댓글 계층 원고 |
| C | 카페명 | 씨씨앙 또는 양평맘 |
| D | 작성계정 | 값이 있으면 정확한 ID 그대로 사용 |
| E | 원고유형 | 질문형 또는 후기형 |
| F | 완료 링크 | 성공 링크 또는 최종 실패 사유 |
| G | 말머리 | 비어 있으면 사용 안 함 |
| H | 계정유형 | D가 비었을 때 실명/비실명 자동 배정 |
| I | 이미지 없음 | Y이면 이미지 미사용 |
| J | 게시판명 | 씨씨앙만 사용: 자유수다방 선택 시 수정 글 이동 |

### 처리 흐름

1. 별도 일상 글 시트에서 해당 카페의 일상 글을 무작위 선택한다.
2. 한 실행 안에서는 일상 글을 중복 사용하지 않는다.
3. V2R API로 일상 글을 5~15분 뒤 예약한다.
4. 씨씨앙 일상 글은 자유수다방(구), `[댓글 이벤트 X]` 말머리(ID 1749), 댓글 허용으로 등록한다.
5. 씨씨앙 +4시간, 양평맘 +20시간에 수정 글을 예약한다.
6. 씨씨앙 J열이 `자유수다방`이면 수정 글을 ID 328로 이동한다.
7. 씨씨앙 J열이 공란이면 수정 글을 자유수다방(구)에 유지한다.
8. 씨씨앙 수정 글은 두 경우 모두 말머리를 제거하고 댓글을 허용한다.
9. 제목·본문·태그·댓글 12개·조회수·예약시간·게시판·말머리를 API로 재조회해 검증한다.
10. 성공 링크를 F열에 기록한다.

일상 글 시트:

```text
https://docs.google.com/spreadsheets/d/1vSON0Rej9anDQXcAOXyBrCr50B4MMqZ79FahF4cDPJw/edit?gid=1842684291
```

### 제휴 댓글 계정

아래 6개를 글마다 무작위 배치한다.

```text
quilliant
hunnede
prtchht
chocobbn
chenallo
colpith
```

- 일반 댓글: 위 계정 무작위 배치
- 대댓글: 본문 작성계정
- 질문형·후기형의 대대댓글 작성 규칙은 `AffiliateApiPublisher._comments()`가 기준
- 원고의 댓글1→2→3→4→5 순서를 반드시 유지
- 계정별 시간 충돌 시 댓글 하나만 이동하지 않는다.
- 해당 글의 댓글 12개 전체를 같은 분만큼 이동한다.
- 등록 후 댓글 내용·시간순서·부모 관계까지 검증한다.

### 제휴 계정 자동 배정

- D열이 비었을 때만 H열 실명/비실명에 맞춰 배정
- V2R 카페 가입·계정 상태와 최근 180일 발행 이력 사용
- 미사용 계정, 가장 오래전에 사용한 계정 순서
- 발행 전 카페별 배정 계정의 멤버등급을 일괄 확인
- 멤버등급이 비어 있으면 V2R `sync/account` API로 한 번 갱신한 뒤 재조회
- 갱신 실패 계정의 행만 실패 처리하고 다른 계정·행은 계속 진행
- 로그인 실패·등급 미달이면 다음 계정으로 교체
- 댓글 전용 계정은 본문 작성에서 제외
- 실패한 RESERVED/FAIL 찌꺼기 source 삭제
- F열의 `http://` 또는 `https://` 링크만 완료로 판단하며 실패 문구는 재시도
- 사진 파일은 첨부 전 존재·크기·이미지 디코딩을 검증
- SE-ONE 사진 첨부 실패 시 화면을 새로 열고 해당 원고의 모든 사진을 최대 3회 재첨부
- Drive에서 고른 파일은 해당 실행 동안 고정해 재시도해도 다른 사진으로 바꾸지 않음
- 모든 사진 구성요소를 준비한 뒤에만 일상·수정 예약 source를 생성
- Drive 또는 이미지 준비 실패도 사진 없는 글로 계속하지 않고 발행 중단
- 최종 실패 화면은 로컬 `logs` 폴더에 PNG로 저장하며 사진 없는 글은 등록하지 않음
- GUI `로그 메모장` 버튼으로 `logs/v2r-auto.log`를 바로 열 수 있음

---

## 프로그램 2: 자사 카페 직접/예약 발행

### 실행 파일과 진입점

- EXE: `V2R-Immediate-Posting.exe`
- 진입점: `main_immediate.py`
- GUI: `v2r_auto/gui_immediate.py`
- API 게시: `v2r_auto/immediate_api.py`
- 입력 파서: `v2r_auto/immediate_inputs.py`
- 실행 제어: `ImmediateRunner` (`v2r_auto/runner.py`)
- 빌드: `v2r-immediate-posting.spec`
- 로컬 데이터: `%LOCALAPPDATA%\V2RImmediatePosting`

### 자사 카페

| 카페 | ID |
|---|---:|
| 쌍둥이맘 모여라 | 10174516 |
| 고요한 아침 | 14567700 |
| 글로시 마이 | 15175096 |
| 웨딩 노트 | 15441090 |
| 송도포털 | 16149995 |
| 헬씨 트리 | 23708088 |
| 러브 인썸 (Love in Some) | 26616683 |
| 마이 웨딩 드림 | 26680163 |

### 한 줄 노출 테스트 카페

| 카페 | ID | 게시판 |
|---|---:|---|
| 태극마케팅센터 | 31670254 | 자유게시판 |
| 소나무마케팅센터 | 31670256 | 자유게시판 |

### 카페·게시판 API 매칭

- 실행 때 `/naver_cafes/naver_join_cafes`, `/naver_cafes/naver_join_cafe`, `/naver_cafes/menus`를 사용한다.
- 등록된 자사 카페는 화면 표시명이 바뀌어도 검증된 카페 ID를 우선 사용한다.
- 공백·이모지·장식 문자를 제거한 정확한 이름을 우선한다.
- 정확 일치가 없으면 한글 이름이 유일한 경우에만 매칭한다.
- 비슷한 후보를 임의로 선택하지 않는다.
- 모든 정상 계정의 게시판 응답을 합쳐 게시판 ID를 찾는다.
- 확인된 별칭:

```text
쌍둥이맘 모여라: 가족업체 자유게시판 → ㄴ가족업체 자유게시판 (ID 664)
마이 웨딩 드림: 웨딩홀 탐방기 → 웨딩홀 탑방기 (ID 5)
```

### 모드 A: 브랜드 Google Sheet

열 구조:

| 열 | 헤더 |
|---|---|
| A | 키워드 |
| B | 본문 |
| C | 카페명 |
| D | 작성계정 |
| E | 원고유형 |
| F | 완료 링크 |
| G | 말머리 |
| H | 계정유형 |
| I | 이미지 없음 |
| J | 게시판명 |

- 댓글이 있으면 기존 질문형/후기형 댓글 12개 배치
- 댓글이 없으면 글만 발행
- 브랜드 원고의 본문 줄바꿈·문장부호는 원문 그대로 유지
- V2R AI 댓글은 반드시 `use_comment_ai: false`
- D가 비면 H의 실명/비실명 조건으로 자동 배정
- D/F 기록 실패는 V2R 발행 자체를 중단하거나 중복 재발행시키지 않는다.

### 모드 B: Excel 일상 글

Excel은 열 위치가 아니라 아래 헤더 이름으로 읽는다:

```text
필수: 카페명 / 게시판명 / 각색제목 / 각색본문
선택: 게시판링크 / 작성계정 / 등록시각
```

- 작성계정 값이 있으면 해당 ID를 우선 사용
- 작성계정이 비어 있으면 실명 계정을 자동 배정
- 게시판링크가 있으면 URL의 카페 ID·게시판 ID를 이름보다 우선 검증
- 여러 카페가 있으면 카페별 원래 글 순서를 유지하며 교차 처리
- 한 카페 글만 남은 경우에만 같은 카페가 연속될 수 있음
- 댓글 없음
- 자사 카페에서는 카페별 누적 5~15분 간격 예약
- 같은 카페의 다음 글은 이전 예약시간 +5~15분
- 테스트 카페가 지정된 한 줄 글은 예약하지 않고 즉시 발행

### 모드 C: 정보성 키워드 Google Sheet

고정 시트:

```text
Spreadsheet ID: 1vSON0Rej9anDQXcAOXyBrCr50B4MMqZ79FahF4cDPJw
gid: 1193993260
```

- 열은 브랜드 시트와 동일
- 댓글 없음
- 정보성 시트와 Excel 일상 글에만 본문 자동 정리 적용
- 브랜드별 Google Sheet에는 자동 정리 미적용
- V2R AI 댓글은 기존대로 사용

본문 자동 정리:

- 일반 쉼표·마침표·말줄임표 제거
- `37.5`, `1,500`처럼 숫자 사이 기호 유지
- 그림형 이모지 제거
- `ㅠㅠ`, `ㅎㅎ`, `ㅋㅋ`, `!`, `?`, `~` 유지
- 기존 줄바꿈 유지
- 한국어 종결 표현으로 문장 인식
- 3문장 이상: 한 문장 한 줄, 두 문장마다 빈 줄
- 긴 2문장: 두 문장 사이 빈 줄
- 원고 형식 오류 한 행이 전체 배치를 중단하지 않음

### 모드 D: 전체 계정 한 줄 테스트

고정 시트:

```text
Spreadsheet ID: 1UgcAvHFCpC5N9joC9T5WCATK834F3XAtRrepFv6XbEs
gid: 218285244
```

헤더:

```text
번호 / ID / 작업 구분 / 연동 / 가아사 조건 / 실/비실 /
테스트 선택 / 테스트 결과 / 테스트 링크 / 테스트 일시
```

- `테스트 선택` 체크박스가 TRUE인 행만 처리
- 모든 선택 계정을 검사하며 실명/비실명·매니저·댓글 역할로 제외하지 않는다.
- V2R 미등록이면 발행하지 않고 사유 기록
- 테스트 카페 둘 다 미가입이면 `테스트 카페 미가입`
- 하나만 가입하면 해당 카페 사용
- 둘 다 가입하면 태극/소나무에 번갈아 배정
- 자유게시판에 즉시 발행
- V2R AI 댓글 미사용

생성 내용:

```text
제목: 김천kb보험 그라래{A열 번호}

본문:
김천kb보험 그라래{A열 번호}
김천kb보험 그라래{A열 번호}
```

결과 기록:

- 실패: `테스트 결과` 헤더 열에 사유만 기록
- 성공: `테스트 결과`, `테스트 링크`, `테스트 일시` 헤더 열에 기록
- `테스트 선택` 체크박스는 프로그램이 변경하지 않음
- 모든 ID 검사를 먼저 완료한 후 시트 결과를 기록
- 이전 실행 성공 이력이 있어도 선택된 계정은 매번 새 글을 발행하고 결과·링크·일시를 최신 값으로 덮어씀

### 자사 댓글 계정

브랜드 수동 댓글에서 아래 6개를 무작위 사용한다.

```text
repalbass
jeehashed
maritane
skabakc
uatrayb
rowpalse
```

### 자사 본문 계정 규칙

- 카페 전체에서 미사용/가장 오래 사용하지 않은 순서 하나를 유지
- 게시판마다 순서를 처음부터 다시 시작하지 않는다.
- 선택한 게시판을 사용할 수 없는 계정만 건너뛴다.
- Excel 일상 글은 실명 계정만
- 브랜드 시트는 H열 실명/비실명 조건
- GUI에서 카페·계정유형별 자동 배정 ID 수를 2~10개로 제한 가능
- 직접 지정한 작성계정은 자동 배정 ID 수에 포함하지 않음
- 다음 매니저 계정은 본문 작성에서 제외:

```text
redsagua01
clktrade
```

- 제휴·자사 댓글 전용 계정도 본문 작성에서 제외
- 발행 전 카페별 배정 계정의 멤버등급이 비어 있으면 `sync/account` API로 갱신

### 예약·일시정지

- 일반 자사 원고와 Excel은 GUI에서 예약 발행 또는 즉시 발행 선택
- 즉시 발행은 GUI에서 글 사이 간격을 1~15분으로 선택
- 자사 카페 첫 글: 실행 시각 +5~15분
- 같은 카페 다음 글: 이전 예약시간 +5~15분
- 카페별 예약시간 독립
- 일시정지 후 다음 미처리 행부터 계속
- 아직 유효한 예약시간은 그대로 유지
- 다음 예약이 2분 미만으로 가까워진 경우에만 해당 카페의 남은 체인을 최소 조정
- 즉시 발행 선택 시 `start_at` 없이 등록하고 실제 DONE 상태까지 확인
- 테스트 카페 한 줄 글은 항상 즉시 발행

### 제한 계정과 실제 결과

- 예약 등록 성공은 실제 발행 성공이 아니다.
- 예약 등록은 `예약대기`로 표시한다.
- 다음 실행에서는 `immediate-history.json`과 현재 시트에 저장된 source만 확인한다.
- 카페 전체의 과거 실패 이력은 조회하거나 반복 표시하지 않는다.
- 현재 작업과 연결된 실제 실패·삭제 source만 완료 이력에서 제거해 재시도한다.
- 코드 27000 감지 계정은 최초 발견일부터 30일간 전역 제외한다.
- 같은 실패 source를 다시 읽어도 30일이 계속 연장되지 않는다.
- 30일 후 자동 복귀한다.
- 한 줄 테스트 `테스트 결과` 표시 예:

```text
코드 27000 활동 제한 (발견 2026-08-12 / 제외 종료 2026-09-11)
```

주요 오류:

| 오류 | 의미 | 처리 |
|---|---|---|
| NAVER_LOGIN_FAIL | 네이버 로그인 실패 | 다른 계정 또는 결과 기록 |
| 27000 | 네이버 카페 글쓰기 활동 제한 | 발견일부터 30일 제외 |
| 20004 | 연속 글 등록 제한 | 일시적 실패 |
| MAX_ALLOWED_CREATE_NAVER_CAFE_ARTICLE (42) | V2R 동시 예약 슬롯 한도 | 슬롯이 빈 뒤 동일 파일 재실행 |

### 중복 이력

- 파일: `%LOCALAPPDATA%\V2RImmediatePosting\immediate-history.json`
- 기본 키: 카페 + 게시판 + 제목 + 본문
- 한 줄 테스트 키에는 정확한 계정 ID도 포함
- 동일 Excel 재실행 시 예약/성공한 글은 건너뛰고 등록 실패 글만 재시도
- 한 줄 테스트는 과거 성공 이력과 관계없이 선택할 때마다 새로 발행
- 제휴 프로그램도 `jobs.db`에 저장된 일상·수정 source만 확인하며 카페 전체
  실패 source를 사전 삭제하지 않는다.

### Google Sheet 쓰기

- Google Sheets 공식 API가 아니라 로그인된 Chrome의 선택 셀 입력을 사용한다.
- 활성 편집기 또는 선택 셀 키보드 입력을 사용하고 CSV export로 값을 검증한다.
- D/F는 최대 3회 재시도
- D 저장 실패여도 이미 선택된 메모리 계정으로 V2R 발행 계속
- 발행 후 D/F 재시도
- 시트 저장 실패는 V2R 재발행 원인이 되어서는 안 된다.
- 한 줄 테스트는 모든 계정 처리를 먼저 끝낸 후 결과를 기록한다.

---

## 이미지 규칙

Google Drive 루트:

```text
https://drive.google.com/drive/folders/13ouLpDi-mSJctFuw3iKCg-z4FZLPjKiK
```

- 일상 글에는 이미지 없음
- 브랜드 원고 수정/직접 발행에 적용
- 시트 제목 괄호의 브랜드명과 Drive 브랜드 폴더 연결
- `{폴더명}` → 해당 브랜드 하위 폴더에서 무작위 이미지
- 팥순이 `{키워드}` → `팥순이/키워드`에서 A열 키워드와 파일명 공백 제거 일치
- 팥순이 `{B/A}` → `팥순이/BA`에서 무작위
- I열 이미지 없음=Y이면 이미지 미사용
- 이미지가 없거나 Drive 오류여도 텍스트 글은 발행
- 최종 본문에서는 모든 `{...}` 표시 제거

---

## 공통 기술 구조

| 파일 | 역할 |
|---|---|
| `v2r_auto/affiliate_api.py` | 제휴 API 발행, 댓글, 계정, 검증 |
| `v2r_auto/immediate_api.py` | 자사 API 발행, 목적지, 계정, 테스트 |
| `v2r_auto/browser.py` | Chrome 로그인 연결, 시트 쓰기, 이미지 업로드 |
| `v2r_auto/runner.py` | 배치 실행, 재시도, 예약, 일시정지 |
| `v2r_auto/immediate_inputs.py` | Excel/브랜드/정보성/계정테스트 입력 |
| `v2r_auto/cafe_catalog.py` | 카페·게시판 이름/ID 매칭 |
| `v2r_auto/images.py` | Drive 폴더·파일 선택 |
| `v2r_auto/state.py` | 제휴 SQLite 체크포인트·인스턴스 잠금 |
| `v2r_auto/history.py` | 자사 중복 이력 |
| `v2r_auto/restrictions.py` | 코드 27000 30일 제외 |

V2R 방식:

- 글 등록은 Selenium 화면 클릭이 아니라 V2R 내부 API 직접 호출
- 로그인된 Chrome에서 Authorization을 안전하게 캡처
- 이미지 업로드에 필요한 SmartEditor만 로그인 Chrome 활용
- 토큰·쿠키·비밀번호를 파일에 저장하지 않음

---

## 테스트와 빌드

```text
python -m pytest
python -m PyInstaller --noconfirm --clean v2r-auto-affiliate.spec
python -m PyInstaller --noconfirm --clean v2r-immediate-posting.spec
```

Windows CI:

```text
.github/workflows/build-windows.yml
```

실행 파일 배포 브랜치:

```text
cursor/image-upload-downloads-3901
```

다운로드 전달 규칙:

1. 위 배포 브랜치의 최신 전체 커밋 SHA를 확인한다.
2. `blob/<전체 커밋 SHA>/downloads/<파일명>.zip` 파일 페이지 링크를 제공한다.
3. 사용자는 GitHub 로그인 탭에서 파일 페이지를 열고 `Download raw file`을 누른다.
4. 채팅 직접 파일, 임시 업로드, `?raw=1`, GitHub Actions Artifact는 사용하지 않는다.
5. 워크플로는 Artifact를 업로드하지 않고 검증된 ZIP을 배포 브랜치에 커밋한다.

---

## 아직 완전히 자동화되지 않은 부분

1. V2R 오류 42 예약 슬롯 한도 발생 시 로컬 대기열이 자동으로 슬롯을 기다리는 기능은 아직 없다.
   - 현재는 성공한 예약이 발행된 후 동일 Excel을 다시 실행한다.
   - 성공/예약 이력은 건너뛰고 등록 실패 행만 다시 시도한다.
2. Google Sheet 쓰기는 로그인 Chrome UI 의존이라 Google 화면 변경·포커스 문제 가능성이 있다.
   - V2R 발행 성공과 시트 기록 실패를 분리해야 한다.
3. 이 저장소에는 참고 카페 수집·AI 각색 생성기가 없다.

---

## 새 채팅 첫 메시지 예시

```text
아래 저장소와 브랜치를 먼저 확인해줘.

저장소:
https://github.com/silas-netizen/v2r-auto-posting

기준 브랜치:
cursor/immediate-cafe-api-f548

먼저 PROGRAM_HANDOFF.md를 처음부터 끝까지 읽고,
제휴 카페와 자사 카페 기존 기능을 유지한 상태에서만 수정해줘.
EXE를 역분석하지 말고 Python 소스를 기준으로 작업해줘.
일상 글 수집·각색 생성 프로그램은 이 작업 범위에서 제외해줘.
```
